node_modules/
dist/
.wrangler/
.dev.vars
worker-configuration.d.ts
.worktrees/

# Font binaries -- regenerated by `pnpm run build:fonts` from @fontsource.
# src/ui/font-faces.ts IS committed so the repo typechecks without a build.
public/fonts/
